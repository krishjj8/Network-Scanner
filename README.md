# Network-Scanner
A network scanner built with Scapy in Python to discover active hosts, perform port scans, and detect services in a local network. Features include host discovery, port scanning, and OS fingerprinting for network security analysis.
